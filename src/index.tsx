import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';

ReactDOM.render(
  <React.StrictMode>
    <h1>Hello React!</h1>
  </React.StrictMode>,
  document.getElementById('root')
);
